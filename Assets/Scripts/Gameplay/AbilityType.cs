namespace Gameplay
{
    public enum AbilityType
    {
        Bomb,   // clears a 3x3 area centred on the ability piece
        Laser,  // single-tile beam; rotates between horizontal (row) and vertical (column) clear
    }
}
